// Copy this file as config.js in the same folder, with the proper database connection URI.

module.exports = {
  db: 'mongodb production connection string',
  db_dev: 'mongodb development connection string',
};
